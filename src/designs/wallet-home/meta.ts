import { defineDesign } from "../types";
import cover from "./cover.png";

export default defineDesign({
  title: "Wallet home",
  description: "Balance, card, quick actions and recent activity on one dark screen.",
  date: "2026-09-11",
  tags: ["mobile"],
  image: cover,
  notes: `Sample shot, rendered from a mockup so the section is not empty. Replace it with your own: drop a PNG next to meta.ts and point the import at it.`,
});
